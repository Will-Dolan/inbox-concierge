# Inbox Concierge Frontend

React + Vite frontend for Inbox Concierge.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE` in `.env` if the API is not running at `http://localhost:8000`.

## Checks

```bash
npm run lint
npm run build
```
